#!/bin/sh

__cbdpd(){
    while [ $# -gt 0 ] ; do
        case "$1" in
            check)      _cbdpd_check "$@"
                        exit
                        ;;
              job)		_cbdpd_job "$@"
                        exit
                        ;;
        esac
        shift
    done
}

